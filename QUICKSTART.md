# Quickstart (5 min)

1. Run hourly audit:
   - `python3 scripts/hourly_audit.py`
2. Run hooks:
   - `python3 hooks/run_hooks.py`
3. Check outputs:
   - `reports/hourly-status.md`
   - `reports/action-queue.md`
4. Iterate with small commits per cycle.
